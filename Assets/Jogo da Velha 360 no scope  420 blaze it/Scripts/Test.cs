using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Test : MonoBehaviour
{
    /* test array copy
    int[] arraytest;
    // Start is called before the first frame update
    void Start()
    {
        int[] arrayCopy;
        arraytest = new int[9];
        for (int i = 0; i< arraytest.Length ;i++ )
        {
            arraytest[i] = i;
        }
        arrayCopy = new int[arraytest.Length];
        arraytest.CopyTo(arrayCopy, 0);

        for (int i = 0; i < arraytest.Length; i++)
        {
            arrayCopy[i] += i;
            print(arraytest[i] + " " + arrayCopy[i]);
        }
    }
    */
    // Update is called once per frame
    void Update()
    {
        
    }
}
